import initSplide from './splide';

document.addEventListener('DOMContentLoaded', initSplide);

document.addEventListener('livewire:load', initSplide);

document.addEventListener('livewire:navigated', initSplide);
